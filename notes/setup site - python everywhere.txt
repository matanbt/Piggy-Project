-Create Virtual Environment (named - my-virtualenv):
$ mkvirtualenv --python=/usr/bin/python3.8 my-virtualenv

-Install all needed libraries by:
$ pip install flask
OR
$ pip install -r requirements.txt

-Open manual Web, and:
	-Set the virtual environment dir ^
	-Set the source code dir to your web-app dir
	-Edit the WSGI file:
	 import sys
	 path = '/home/matanbt/mysite' <--update 
	 if path not in sys.path:
   	     sys.path.insert(0, path)

	 from web-app import app as application #app is the app var defined in web-app.py <-- update